<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="">
    <%= f.text_field :url, placeholder: "Share article's URL..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-medium btn-primary" %>
<% end %>